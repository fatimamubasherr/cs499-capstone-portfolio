# CS 499 Capstone ePortfolio

Professional GitHub Pages portfolio for Fatima Mubasher's CS 499 Computer Science Capstone.

## Site structure

- `index.html` — portfolio homepage
- `styles.css` — visual styling
- `documents/` — professional self-assessment and enhancement narratives
- `artifacts/` — milestone artifact ZIP files
- `images/` — optional portfolio imagery

## Final setup

Before final submission:

1. Replace `CODE_REVIEW_LINK_HERE` in `index.html` with the actual code review video URL.
2. Confirm the three artifact ZIP filenames exactly match the filenames used in `index.html`.
3. Confirm the four Word document filenames exactly match the filenames used in `index.html`.
4. Enable GitHub Pages from the repository's `main` branch and `/ (root)` folder.
